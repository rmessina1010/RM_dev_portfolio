import React from 'react';
import { Row, Col, Card, CardBody, CardDeck, CardImg, Button, CardHeader, CardTitle } from 'reactstrap';

export function Project(props) {

    return (
        <Card>
            <CardHeader>
                <CardTitle>Project Title</CardTitle>
                <CardImg />
            </CardHeader>
            <CardBody>
            </CardBody>
        </Card>
    )
}